# 作业2：基于LangChain和RAG的学业预警知识问答

> **学号：** （待填）　**姓名：** （待填）　**日期：** 2026年6月

---

## 一、技术栈说明

| 模块 | 技术方案 | 说明 |
|------|----------|------|
| **文档加载** | `PyPDFLoader`（langchain_community） | 加载 PDF 格式的《学业预警实施办法》文档，逐页解析文本 |
| **文档切分** | `RecursiveCharacterTextSplitter`（langchain） | 智能递归切分，chunk_size=500，overlap=80，分隔符优先级：`\n\n` → `\n` → `。` → `；` → `，` → ` ` |
| **Embedding 模型** | `BAAI/bge-small-zh-v1.5`（HuggingFaceEmbeddings） | 中文语义向量化模型，将文本块转换为 512 维向量 |
| **向量数据库** | `Chroma`（langchain_chroma） | 轻量级向量数据库，支持本地持久化存储，避免重复计算 Embedding |
| **关键词检索** | `BM25Retriever`（langchain_community） | 基于 BM25 算法的关键词匹配检索，补充向量检索的词汇匹配能力 |
| **混合检索** | `EnsembleRetriever`（langchain.retrievers） | 加权融合向量检索与 BM25 检索结果，权重各 0.5，各取 Top-10 |
| **重排序模型** | `Cross-Encoder`：`BAAI/bge-reranker-base`（sentence_transformers） | 对混合检索结果进行精排，计算 query-document 相关性分数，取 Top-5 |
| **大语言模型** | `Ollama` + `qwen3:8b`（langchain_ollama, ChatOllama） | 本地部署的通义千问3（8B参数），temperature=0.1 确保回答稳定性 |
| **提示词工程** | `ChatPromptTemplate`（langchain_core） | System Prompt 严格约束：只能使用参考资料信息，不得编造，资料无相关信息时明确告知 |
| **Web 界面** | `Gradio 5.9` | 提供交互式 Chatbot UI，支持示例问题一键提问，对话历史管理 |

### RAG 处理流程

```
用户提问 → 混合检索(向量+BM25, 各取Top-10) → Cross-Encoder重排序(Top-5) → LLM生成回答 → 返回结果
```

---

## 二、核心代码

### 2.1 RAG 核心系统（rag_system.py）

```python
"""
学业预警知识问答系统 - 核心RAG模块
基于 LangChain 框架，实现 PDF加载→文档切分→向量化→混合检索→重排序→LLM生成 的完整流程
"""

import os
from typing import List, Tuple

from langchain_community.document_loaders import PyPDFLoader
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain_community.embeddings import HuggingFaceEmbeddings
from langchain_chroma import Chroma
from langchain_community.retrievers import BM25Retriever
from langchain.retrievers import EnsembleRetriever
from langchain_ollama import ChatOllama
from langchain_core.documents import Document
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
from langchain_core.runnables import RunnablePassthrough

from sentence_transformers import CrossEncoder


class AcademicWarningRAG:
    """学业预警 RAG 问答系统"""

    def __init__(
        self,
        pdf_path: str = "data/软件与人工智能学院本科生学业预警实施办法.pdf",
        persist_dir: str = "data/chroma_db",
        embedding_model: str = "BAAI/bge-small-zh-v1.5",
        reranker_model: str = "BAAI/bge-reranker-base",
        llm_model: str = "qwen3:8b",
        chunk_size: int = 500,
        chunk_overlap: int = 80,
        vector_weight: float = 0.5,
        bm25_weight: float = 0.5,
        top_k_retrieval: int = 10,
        top_k_rerank: int = 5,
    ):
        # ... 参数初始化（详见完整代码） ...

    # ========== 1. 文档加载 ==========
    def load_pdf(self) -> List[Document]:
        """使用 PyPDFLoader 加载 PDF 文档"""
        loader = PyPDFLoader(self.pdf_path)
        docs = loader.load()
        return docs

    # ========== 2. 文档切分 ==========
    def split_documents(self, docs: List[Document]) -> List[Document]:
        """使用 RecursiveCharacterTextSplitter 智能切分文档"""
        splitter = RecursiveCharacterTextSplitter(
            chunk_size=self.chunk_size,
            chunk_overlap=self.chunk_overlap,
            separators=["\n\n", "\n", "。", "；", "，", " ", ""],
            keep_separator=True,
        )
        return splitter.split_documents(docs)

    # ========== 3. 构建向量存储 ==========
    def build_vector_store(self, chunks: List[Document]) -> Chroma:
        """使用 HuggingFace Embedding + Chroma 构建向量存储"""
        self.embeddings = HuggingFaceEmbeddings(
            model_name=self.embedding_model_name,
            model_kwargs={"device": "cpu"},
            encode_kwargs={"normalize_embeddings": True},
        )
        # 持久化到本地，避免重复计算
        if os.path.exists(self.persist_dir) and os.listdir(self.persist_dir):
            self.vector_store = Chroma(
                persist_directory=self.persist_dir,
                embedding_function=self.embeddings,
            )
        else:
            self.vector_store = Chroma.from_documents(
                documents=chunks, embedding=self.embeddings,
                persist_directory=self.persist_dir,
            )
        return self.vector_store

    # ========== 4. 构建混合检索器 ==========
    def build_hybrid_retriever(self, chunks: List[Document]) -> EnsembleRetriever:
        """构建 BM25 + 向量 混合检索器"""
        # 向量检索器
        vector_retriever = self.vector_store.as_retriever(
            search_kwargs={"k": self.top_k_retrieval}
        )
        # BM25 关键词检索器
        self.bm25_retriever = BM25Retriever.from_documents(
            chunks, k=self.top_k_retrieval,
        )
        # 混合检索器（加权融合）
        self.ensemble_retriever = EnsembleRetriever(
            retrievers=[vector_retriever, self.bm25_retriever],
            weights=[self.vector_weight, self.bm25_weight],
        )
        return self.ensemble_retriever

    # ========== 5. Cross-Encoder 重排序 ==========
    def build_reranker(self):
        """加载 Cross-Encoder 重排序模型"""
        self.reranker = CrossEncoder(self.reranker_model_name, max_length=512)

    def rerank(self, query: str, docs: List[Document]) -> List[Document]:
        """使用 Cross-Encoder 对检索结果重排序，返回 Top-K"""
        pairs = [[query, doc.page_content] for doc in docs]
        scores = self.reranker.predict(pairs)
        scored_docs = sorted(zip(docs, scores), key=lambda x: x[1], reverse=True)
        return [doc for doc, score in scored_docs[:self.top_k_rerank]]

    # ========== 6. 构建 LLM 问答链 ==========
    def build_llm_chain(self):
        """使用 Ollama + qwen3:8b 构建问答链"""
        self.llm = ChatOllama(model=self.llm_model, temperature=0.1)

        # 提示词模板：严格约束回答范围
        prompt = ChatPromptTemplate.from_messages([
            ("system",
             "你是广州软件学院学业预警知识问答助手。请严格根据以下参考资料回答问题。\n\n"
             "规则：\n"
             "1. 只能使用参考资料中的信息回答，不得编造任何信息。\n"
             "2. 如果参考资料中没有相关信息，请明确回答「根据现有资料，无法回答此问题」。\n"
             "3. 回答要简洁、准确，引用原文中的关键条款。\n"
             "4. 如果问题涉及计算（如绩点、门数），请仔细对照资料中的条件进行判断。\n\n"
             "参考资料：\n{context}"
             ),
            ("human", "{question}"),
        ])

        def retrieve_and_rerank(query: str) -> List[Document]:
            """混合检索 + 重排序"""
            docs = self.ensemble_retriever.invoke(query)
            return self.rerank(query, docs)

        def format_docs(docs: List[Document]) -> str:
            return "\n\n---\n\n".join(
                f"[来源第{doc.metadata.get('page', '?')}页] {doc.page_content}"
                for doc in docs
            )

        # 构建 LCEL 链
        self.chain = (
            {"context": lambda q: format_docs(retrieve_and_rerank(q)),
             "question": RunnablePassthrough()}
            | prompt | self.llm | StrOutputParser()
        )
        return self.chain

    # ========== 对外问答接口 ==========
    def ask_with_sources(self, question: str) -> Tuple[str, List[Document]]:
        """带检索来源的问答接口"""
        docs = self.ensemble_retriever.invoke(question)
        docs = self.rerank(question, docs)
        context = "\n\n---\n\n".join(
            f"[来源第{doc.metadata.get('page', '?')}页] {doc.page_content}"
            for doc in docs
        )
        prompt = ChatPromptTemplate.from_messages([...])  # 同上提示词
        chain = prompt | self.llm | StrOutputParser()
        answer = chain.invoke({"context": context, "question": question})
        return answer, docs
```

### 2.2 Web UI（app.py 核心部分）

```python
"""学业预警知识问答系统 - Gradio Web UI"""
import gradio as gr
from rag_system import AcademicWarningRAG

EXAMPLE_QUESTIONS = [
    "学业预警工作由谁负责？",
    "什么情况下会被黄色预警？",
    "红色预警的条件是什么？绩点低于多少算红色预警？",
    "绩点1.7，2门必修不及格会收到什么预警？",
    "如果一学期挂了4门必修课，会收到什么预警？会有哪些措施？",
    "我有3门选修课没过，会预警吗？",
]

def build_ui(rag: AcademicWarningRAG):
    with gr.Blocks(title="学业预警知识问答系统", theme=gr.themes.Soft()) as demo:
        # Chatbot 对话区
        chatbot = gr.Chatbot(label="问答对话", type="messages", height=450)
        msg_input = gr.Textbox(label="请输入你的问题", ...)
        
        def chat_fn(question, history):
            answer, sources = rag.ask_with_sources(question.strip())
            # 构建来源信息展示
            new_history = history + [
                {"role": "user", "content": question},
                {"role": "assistant", "content": answer + source_text},
            ]
            return new_history, ""

        msg_input.submit(fn=chat_fn, inputs=[msg_input, chatbot], outputs=[chatbot, msg_input])
    return demo

if __name__ == "__main__":
    rag = AcademicWarningRAG(...)
    rag.initialize()
    demo = build_ui(rag)
    demo.launch(server_name="0.0.0.0", server_port=7860)
```

---

## 三、代码仓库链接

**GitHub 仓库地址：** [https://github.com/spiner111/chenyixing](https://github.com/spiner111/chenyixing)

**项目结构：**
```
chenyixing/
├── .gitignore          # 忽略 pycache、向量库、PDF 等
├── README.md           # 项目说明（技术栈、运行方式、流程图）
├── rag_system.py       # 核心 RAG 系统模块
├── app.py              # Gradio Web UI 主程序
└── requirements.txt    # Python 依赖清单
```

（截图：请打开 GitHub 仓库页面截图粘贴于此）

---

## 四、系统运行效果测试

### 初始化日志

```
[1/6] 正在加载 PDF: data/软件与人工智能学院本科生学业预警实施办法.pdf
      加载完成，共 7 页
[2/6] 正在切分文档 (chunk_size=500, overlap=80)
      切分完成，共 8 个文本块
[3/6] 正在构建向量存储 (embedding=BAAI/bge-small-zh-v1.5)
      新建向量库，持久化到: data/chroma_db
      向量存储就绪
[4/6] 正在构建混合检索器 (vector_weight=0.5, bm25_weight=0.5)
      混合检索器就绪 (Top-10)
[5/6] 正在加载重排序模型: BAAI/bge-reranker-base
      重排序模型就绪
[6/6] 正在加载 LLM: Ollama/qwen3:8b
      LLM 问答链就绪

[OK] 学业预警 RAG 系统初始化完成！
```

### 测试问题与回答

---

**问题1：学业预警工作由谁负责？**
> **回答：** 根据参考资料，学业预警工作的负责人如下：
> 1. **第一责任人**：软件与人工智能学院院长；
> 2. **直接责任人**：分管学生工作的书记和分管教学工作的副院长；
> 3. **具体责任人**：辅导员、学习导师和教务员。
>
> 此外，**教学指导委员会**负责指导实施学业预警工作（见第六条）。
>
> 📄 参考页码：[0, 1, 2, 3]

---

**问题2：什么情况下会被黄色预警？**
> **回答：** 根据参考资料，学生在以下情况下会被黄色预警：
> 1. **一学期中必修课程不及格课程门数≥2门**；
> 2. **入学以来累计不及格课程门数≥4门**；
> 3. **平均绩点<2.0**。
>
> 📄 参考页码：[0, 1, 4, 5]　Rerank最高分：0.8555

---

**问题3：红色预警的条件是什么？绩点低于多少算红色预警？**
> **回答：** 根据资料，红色预警的条件为：
> 1. 一学期必修课程不及格≥4门；
> 2. 入学以来累计不及格课程≥7门；
> 3. **平均绩点<1.5**。
>
> 因此，**绩点低于1.5**即触发红色预警。
>
> 📄 参考页码：[0, 1, 2, 4]　Rerank最高分：0.9941

---

**问题4：绩点1.7，2门必修不及格会收到什么预警？**
> **回答：** 根据资料，绩点1.7且2门必修不及格的情况符合**黄色预警**条件。
> 具体依据：
> 1. **平均绩点<2.0**（黄色预警条件之一）。
> 2. **2门必修不及格**（黄色预警条件之一）。
>
> 因此，学生将被下达**黄色预警**，由辅导员和学习导师进行谈话。
>
> 📄 参考页码：[0, 1, 4, 5]　Rerank最高分：0.9667

---

**问题5：如果一学期挂了4门必修课，会收到什么预警？会有哪些措施？**
> **回答：** 根据资料，一学期挂4门必修课属于**橙色预警**。
> **预警措施包括**：
> 1. 学院向学生下达《学业预警通知书》；
> 2. 辅导员和学习导师对学生进行预警谈话；
> 3. 指导学生制定学习计划。
>
> 📄 参考页码：[0, 1, 2, 4]　Rerank最高分：0.9890

---

**问题6：我有3门选修课没过，会预警吗？**
> **回答：** 根据资料，无法明确回答此问题。
> **原因：**
> 1. 资料中所有预警条件均针对**必修课程**，未提及选修课是否纳入预警计算。
> 2. 资料未明确说明"累计不及格课程门数"是否包含选修课。
>
> 建议咨询学校教务部门以获取更准确的解释。
>
> 📄 参考页码：[0, 1, 4, 5]　Rerank最高分：0.9027

---

### Web UI 界面

（截图：请启动 `python app.py` 后访问 `http://localhost:7860`，截图 Web 界面）

---

## 五、遇到的问题及解决方案

### 问题1：Windows 下 ChromaDB 无法加载 SQLite DLL

**问题描述：**
在 Windows 10 + Anaconda 环境下第一次运行系统时，`from langchain_chroma import Chroma` 报错：
```
ImportError: DLL load failed while importing _sqlite3: 找不到指定的模块。
```

**原因分析：**
Anaconda 的 `_sqlite3.pyd` 依赖 `sqlite3.dll`，而 `sqlite3.dll` 存放在 `<Anaconda>/Library/bin/` 目录下。但当前 shell 的 `PATH` 环境变量中缺少该路径，导致系统无法找到 DLL 文件。

**解决方案：**
在启动 Python 前，手动将 `Library/bin` 加入 PATH：
```bash
export PATH="$PATH:/c/ProgramData/Anaconda3/Library/bin"
python app.py
```
验证：`_sqlite3` 模块成功导入，ChromaDB 正常工作。

**学到的经验：**
Windows 上 Python C 扩展模块的 DLL 依赖查找依赖于操作系统 PATH，Anaconda 的 DLL 目录默认不在系统 PATH 中。未来遇到类似 `ImportError: DLL load failed` 错误，应首先检查 PATH 是否包含相应的 Library/bin 目录。可以将此命令写入 bat 启动脚本一劳永逸。

---

### 问题2：Windows 控制台 GBK 编码导致 Unicode 字符打印错误

**问题描述：**
代码中使用 `✓`（Unicode 2713）和 `📝`（Emoji）等字符进行美化输出，在 Windows GBK 控制台下运行时报错：
```
UnicodeEncodeError: 'gbk' codec can't encode character '✓' in position 2: illegal multibyte sequence
```

**原因分析：**
Windows 中文版默认控制台编码为 GBK（代码页 936），无法编码超出 GBK 字符集范围的 Unicode 符号。而 Python 的 `print()` 会尝试使用控制台编码输出，导致抛出异常。

**解决方案：**
将特殊 Unicode 符号替换为 ASCII 安全字符：
- `✓` → `[OK]`
- `📝` → `>>`

这些替换不影响 Gradio Web UI（浏览器渲染 UTF-8 正常），仅影响 CLI 模式下的控制台输出。

**学到的经验：**
编写跨平台 Python CLI 工具时，应避免在 `print()` 中使用非 ASCII 字符（特别是 Emoji），或者使用以下兼容写法：
```python
import sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
```
但更好的做法是在源码层面使用 ASCII 字符，保持最大兼容性。

---

## 六、学习心得

通过本次作业，我深入理解了 RAG（检索增强生成）技术的完整链路：

1. **检索质量决定回答质量**：混合检索（向量+BM25）比单一检索策略效果更好——向量检索擅长语义匹配（如"挂科"→"不及格"），BM25 擅长精确关键词匹配（如"绩点 1.5"）。Cross-Encoder 重排序进一步提升了 Top-5 文档的相关性。

2. **提示词工程是关键**：通过 System Prompt 严格约束模型"只能使用参考资料，不得编造"，有效抑制了大模型的幻觉问题。对于资料无法回答的问题（如"3门选修课"），模型能诚实地告知无法回答而非胡编。

3. **LangChain 模块化设计的优势**：各个组件（Loader、Splitter、Retriever、LLM）可独立替换和调参，极大提高了开发效率和可维护性。
